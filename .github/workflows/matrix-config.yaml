name: Matric Config

on: push

jobs:
    run-nginx:
        runs-on: self-hosted
        steps:
            - name: Echo Docker Details
              run: docker info

            - name: Run Image
              run: docker run --rm nginx:alpine3.22

    run-httpd:
        runs-on: self-hosted
        steps:
            - name: Echo Docker Details
              run: docker info

            - name: Run Image
              run: docker run --rm httpd:alpine3.23
